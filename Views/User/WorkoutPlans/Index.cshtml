@model IEnumerable<ClientWorkoutPlan>

@{
    ViewData["Title"] = "Your Workout Plans";
    Layout = "_UserLayout";
}

<h1>Your Workout Plans</h1>

<table class="table">
    <thead>
        <tr>
            <th>Plan Name</th>
            <th>Type</th>
            <th>Assigned On</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var userPlan in Model)
        {
            <tr>
                <td>@userPlan.WorkoutPlan.Name</td>
                <td>@userPlan.WorkoutPlan.WorkoutPlanType.Name</td>
                <td>@userPlan.AssignedDate.ToShortDateString()</td>
                <td>
                    <a asp-action="History" asp-route-userWorkoutPlanId="@userPlan.Id"
                       class="btn btn-sm btn-info">View History</a>
                </td>
            </tr>
            @foreach (var workout in userPlan.WorkoutPlan.WorkoutPlanWorkouts)
            {
                <tr>
                    <td colspan="3" style="padding-left: 50px;">
                        @workout.Workout.Name (@workout.Workout.WorkoutExercises.Count exercises)
                    </td>
                    <td>
                        <a asp-action="LogWorkout"
                           asp-route-userWorkoutPlanId="@userPlan.Id"
                           asp-route-workoutId="@workout.WorkoutId"
                           class="btn btn-sm btn-primary">Log Workout</a>
                    </td>
                </tr>
            }
        }
    </tbody>
</table>