@model Workout
@{
    ViewData["Title"] = "Create Workout";
    Layout = "_AdminLayout";
}

<div class="row">
    <div class="col-md-8">
        <h1>Create Workout</h1>

        <form asp-action="Create" method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>

            <div class="form-group mb-3">
                <label asp-for="Name" class="form-label">Workout Name</label>
                <input asp-for="Name" class="form-control" required />
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>

            <div class="form-group mb-3">
                <label class="form-label">Exercises (Select up to 10)</label>
                <select name="selectedExerciseIds" class="form-select" multiple size="10"
                        asp-items="@(new MultiSelectList(ViewBag.Exercises, "Id", "Name"))">
                </select>
                <small class="form-text text-muted">Hold Ctrl/Cmd to select multiple exercises</small>
            </div>

            <div class="form-group mt-4">
                <button type="submit" class="btn btn-primary">Create Workout</button>
                <a asp-action="Index" class="btn btn-secondary">Back to List</a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}