@model IEnumerable<WorkoutLog>
@{
    ViewData["Title"] = "Workout History";
    Layout = "_ClientLayout";
}

<h1>Workout History</h1>

<table class="table">
    <thead>
        <tr>
            <th>Date</th>
            <th>Workout</th>
            <th>Exercises</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var log in Model)
        {
            <tr>
                <td>@log.Date.ToShortDateString()</td>
                <td>@log.Workout.Name</td>
                <td>
                    @foreach (var set in log.SetLogs.GroupBy(s => s.ExerciseId))
                    {
                        var exercise = set.First().Exercise;
                        <div>
                            <strong>@exercise.Name</strong>:
                            @string.Join(", ", set.Select(s => $"{s.Reps} reps × {s.Weight}kg"))
                        </div>
                    }
                </td>
            </tr>
        }
    </tbody>
</table>

<div>
    <a asp-action="Index">Back to Plans</a>
</div>